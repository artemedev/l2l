[[_TOC_]]

# 8.6.1-preview.1
Устнавливаем/разрываем соединение при начале/останове работы, а не при конфигурации/удалении объекта
# 8.6.0
Добавлен отдельный интерфейс печати  напрямую из ZPL
# 8.5.2
Обновлены используемые пакеты
# 8.5.1
Удален анализ ответов от принтера по статусу
# 8.5.0
добавлена возможность печати из файлов
# 8.4.5
Исправлен расчет размера буфера
# 8.4.2
Удален ошибочный код 94
# 8.4.1
Исправлено пространство имемн

# 8.4.0
Вынеceны настройки DM в конфиг принтера

# 8.3.0
Обновилось DTO до версии 8.9.0.

# 8.2.0
- Обновилось DTO до версии 8.8.0.
- Переименован класс `MD.Aggregation.Devices.Printers.Base` на `MD.Aggregation.Devices.Printers.Printer`
- В абстрактном классе `MD.Aggregation.Devices.Printers.Printer` реализовано поле `Purpose = FunctionalPurpose.Printer`
- В абстрактном классе `MD.Aggregation.Devices.Printers.Printer` реализован расчетный счетчик использования буфера.

# 8.1.0
В общем привели принтер в соответствии с DTO

# 8.0.0

- Удален старый класс `Printer`
- Изменено корневое пространство имен c `MD.Aggregation.Devices.Zebra` на `MD.Aggregation.Devices.Printers`
- Изменено имя сборки/пакета с `MD.AGGR.Devices.Zebra.Printers` на `MD.AGGR.Devices.Printers`
- Создан класс `Base`содержимое которого взято из класс `Printer` (проект `Сервис агрегации`).
- Создано пространство имен `ZPL` (каталог проекте с именем `ZPL`)
- В каталоге `ZPL` создан класс `Printer`, содержимое которого взято из класс `PrinterZPL` (проект `Сервис агрегации`). Наследуется класс от `MD.Aggregation.Devices.Printers.Base`
- В каталоге `ZPL` создан класс `GetStatusWorker`, содержимое которого взято из класс `ZPLGetStatus` (проект `Сервис агрегации`).
- В класс `PrinterConfig` добавлено `public Configuration? configuration { get; set; }`

